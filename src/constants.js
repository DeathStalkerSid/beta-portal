import { createBrowserHistory } from 'history';

export const CONSTANTS = {
	url: 'https://61d333e5b4c10c001712b85e.mockapi.io/api/v1/',
	passwordRegex: /(?=.*[a-z])(?=.*[A-Z])/
};

export const history = createBrowserHistory();